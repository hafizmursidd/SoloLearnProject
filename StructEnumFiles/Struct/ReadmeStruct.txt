

1.) Struct used to encaptulate a small group of related variables
2.) Struct don't support inheritance and cannot contain virtual method.
3.) struct can be intansiasi without new operator
4.) Struct can contain, methods, properties, indexer, so on. struct can not contain defafult constructor but can have constructor with parameters