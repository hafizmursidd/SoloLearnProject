

1.) Interface: Interfaces can contain properties and methods, but not fields.
2.)  An interface is a completely abstract class, which contains only abstract members.
	It is declared using the interface keyword: All members of the interface are by default abstract, so no need to use the abstract keyword.
3.) Why use interfaces rather than abstract classes?
	A class can inherit from just one base class, but it can implement multiple interfaces!
4.) 